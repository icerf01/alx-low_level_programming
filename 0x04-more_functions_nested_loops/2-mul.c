#include "main.h"

/**
 * mul - function to multiply 2 integers
 * @a: user input
 * @b: user input
 * @r: stores result
 * Return: return result r
 */
int mul(int a, int b)
{
	int r;

	r = a * b;

	return (r);
}
