#include "main.h"

/**
 * _strlen_recursion - function to
 * get string length
 * @s: char pointer
 * Return: return int str length
 */
_strlen_recursion(char *s)
{
	int i = 0;

	if(s*)
		i++;
	return (i);
}
